name: Validate
description: 'Validate project by running linting, formatting, and testing'

on:
  push:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - name: Validate
      uses: quintenroets/workflows/validate@main
